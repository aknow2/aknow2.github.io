<section class="hero">
	<h1 class="hero__title">aKnow2</h1>
</section>

<style>
	.hero {
		text-align: center;
		padding: 48px 0 24px;
	}

	.hero__title {
		margin: 0;
		font-size: clamp(2.6rem, 5vw, 3.6rem);
		font-weight: 900;
		letter-spacing: 0.04em;
		color: #0b1220;
		background-image: linear-gradient(90deg, #0b1220 0%, #475569 70%, #0b1220 100%);
		-webkit-background-clip: text;
		background-clip: text;
		-webkit-text-fill-color: transparent;
		text-shadow: 0 8px 22px rgba(15, 23, 42, 0.12);
	}
</style>
