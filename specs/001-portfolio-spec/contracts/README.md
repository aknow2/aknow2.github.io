# Contracts

This feature is a static site with no runtime API endpoints. The primary contract is the JSON schema used to define the card catalog.

- [release-catalog.schema.json](release-catalog.schema.json): JSON schema for the Release Catalog data.
